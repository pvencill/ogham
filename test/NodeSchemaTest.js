var NodeSchemaFactory = require('../lib/NodeSchema'),
    should = require('should');

describe('NodeSchemaFactory', function(){



/*    describe('#constructor', function(){
        var name = 'Bob',
            dob = new Date(),
            weight = 123;

        it('should work with no args', function(){
            var bob = new Actor();
            should.strictEqual(undefined, bob.name);
            should.strictEqual(undefined, bob.dob);
        });

        it('should work with expected args', function(){
            var bob = new Actor({
                name: name,
                dob: dob
            });
            should.strictEqual(name, bob.name);
            should.strictEqual(dob, bob.dob);
        });

        it('should work with extra args', function(){
            var bob = new Actor({
                name: name,
                dob: dob,
                weight: weight
            });
            should.strictEqual(name, bob.name);
            should.strictEqual(dob, bob.dob);
            should.strictEqual(weight, bob.weight);
        });
    })*/

})